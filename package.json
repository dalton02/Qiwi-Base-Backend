{  
  "scripts": {
    "dev": "swag init && nodemon --exec go run ./main.go --signal SIGTERM"
  },
  "devDependencies": {
    "prisma": "^5.19.1"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "nodemon": "^3.1.6"
  }
}
